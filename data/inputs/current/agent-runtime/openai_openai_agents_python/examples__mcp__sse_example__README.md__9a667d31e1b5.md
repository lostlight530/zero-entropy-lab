PROVENANCE: {"confidence": 1.0, "entity_id": "doc_openai_openai_agents_python_examples_mcp_sse_example_readme_md_9a667d31e1b5", "primary_owner": "zero", "retrieved_at": "2026-07-11T06:08:48.026690+00:00", "source_path": "examples/mcp/sse_example/README.md", "source_repo": "openai/openai-agents-python", "source_sha": "9a667d31e1b5629ad9c7415bafff69ac296a40e4"}

# Source Document

# MCP SSE Example

This example uses a local SSE server in [server.py](server.py).

Run the example via:

```
uv run python examples/mcp/sse_example/main.py
```

## Details

The example uses the `MCPServerSse` class from `agents.mcp`. The server runs in a sub-process at `https://localhost:8000/sse`.


# Document Diff

```diff
--- previous

+++ 9a667d31e1b5629ad9c7415bafff69ac296a40e4

@@ -0,0 +1,13 @@

+# MCP SSE Example
+
+This example uses a local SSE server in [server.py](server.py).
+
+Run the example via:
+
+```
+uv run python examples/mcp/sse_example/main.py
+```
+
+## Details
+
+The example uses the `MCPServerSse` class from `agents.mcp`. The server runs in a sub-process at `https://localhost:8000/sse`.
```
