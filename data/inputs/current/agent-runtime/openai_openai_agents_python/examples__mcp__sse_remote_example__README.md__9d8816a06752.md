PROVENANCE: {"confidence": 1.0, "entity_id": "doc_openai_openai_agents_python_examples_mcp_sse_remote_example_readme_md_9d8816a06752", "primary_owner": "zero", "retrieved_at": "2026-07-11T06:08:48.492910+00:00", "source_path": "examples/mcp/sse_remote_example/README.md", "source_repo": "openai/openai-agents-python", "source_sha": "9d8816a06752316f9f46cb397f4cd3a4fba997cd"}

# Source Document

# MCP SSE Remote Example

Python port of the JS `examples/mcp/sse-example.ts`. By default it starts the bundled local SSE MCP server and lets the agent use those tools. Set `MCP_SSE_REMOTE_URL` to try a compatible remote SSE server instead.

Run it with:

```bash
uv run python examples/mcp/sse_remote_example/main.py
```

Prerequisites:

- `OPENAI_API_KEY` set for the model calls.


# Document Diff

```diff
--- previous

+++ 9d8816a06752316f9f46cb397f4cd3a4fba997cd

@@ -0,0 +1,13 @@

+# MCP SSE Remote Example
+
+Python port of the JS `examples/mcp/sse-example.ts`. By default it starts the bundled local SSE MCP server and lets the agent use those tools. Set `MCP_SSE_REMOTE_URL` to try a compatible remote SSE server instead.
+
+Run it with:
+
+```bash
+uv run python examples/mcp/sse_remote_example/main.py
+```
+
+Prerequisites:
+
+- `OPENAI_API_KEY` set for the model calls.
```
